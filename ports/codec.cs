using System;

class Codec : IPacketReceived{

    public void handlePacket(int portID, Byte[] bytes){
        //convert the byte packet into a command. 

        // call the command received function. 
    }
    

}